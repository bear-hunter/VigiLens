# Core Flask and Web Server
Flask
python-dotenv         # Manages environment variables from a .env file for configuration
Flask-Cors            # Handles Cross-Origin Resource Sharing to allow the React app to communicate with the backend

# AI and Machine Learning
ultralytics           # The official package for running YOLO models, including YOLOv11-pose
torch                 # The core deep learning framework required by Ultralytics
torchvision           # Companion library for PyTorch for vision tasks

# Video and Data Processing
opencv-python-headless # For all video capture and frame processing (headless version is best for servers)
numpy                 # For efficient numerical operations on pose keypoint data

# Database
Flask-SQLAlchemy      # For creating and managing the incident log database